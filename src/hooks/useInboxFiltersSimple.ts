'use client';

// Simple filters hook - we'll use the main context instead of this separate one
export function useInboxFilters() {
  // This is a placeholder - the main TasksContext handles filters
  throw new Error('Use useTasks() from TasksContext instead');
}